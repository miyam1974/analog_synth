# NEXUS OSC — UI 機能仕様（SPEC）

本ドキュメントは **現在の UI に表出している機能** をカテゴリ別に網羅する。
実装詳細は [ARCHITECTURE.md](ARCHITECTURE.md)、入門は [README.md](README.md) を参照。

**対象バージョン**: アプリ `0.1.0`（`CMakeLists.txt` の `project(VERSION)`） / ウィンドウ初期サイズ **1080×680**

**範囲**: 画面上のコントロール・表示・演奏入力。**UI に無い機能**（オーディオデバイス設定、VST3、ピッチベンド等）は末尾に一覧する。

---

## UI 構成（上から下）

| 領域 | 内容 |
| ---- | ---- |
| ヘッダー | タイトル「NEXUS OSC」＋サブタイトル「ANALOG SIGNAL ENGINE // POLY 16」（同一行、高さ 28px） |
| マスター行 | ALL OFF / MONO / PRESET / SAVE / SAVE AS / LOAD / RESET / DIFF / MASTER |
| モジュールパネル | OSCILLATOR / MIXER / FILTER / AMPLIFIER / LFO（左→右） |
| SYSTEM フッター | MIDI IN、ステータス行（ヘルプ / MIDI 状態） |
| 鍵盤行（下部） | 左→右: **仮想キーボード** / **ト音記号五線譜**（幅 162px）/ **PC キーボード欄**（ON/OFF + キー図、幅 188px） |

モジュール幅（参考）: OSC **21%** / MIXER **17%** / FILTER **23%** / AMP **19%** / 残り LFO。

---

## 1. グローバル（マスター行）

| UI | 種別 | 説明 | パラメータ / 動作 |
| -- | ---- | ---- | ----------------- |
| **ALL OFF** | ボタン | 全発音の即時停止（パニック） | 全ボイス停止、仮想キーボード状態リセット、MIDI キュークリア |
| **MONO** | トグル | モノフォニック演奏 | ON: 同時 1 音（レガート・GLIDE 向け）。OFF: 16 ボイスポリ |
| **PRESET** | コンボ | プリセット選択 | 内蔵 4 種 + ユーザープリセット名を一覧表示 |
| **SAVE** | ボタン | 読み込み中ユーザープリセットの上書き | 編集後かつユーザープリセット選択時のみ有効。名前は読み取り専用ダイアログ |
| **SAVE AS** | ボタン | 別名保存 | 名前入力ダイアログ → JSON 新規保存。編集後に有効（内蔵プリセット編集中も可） |
| **LOAD** | ボタン | ユーザープリセット読込 | ファイル選択ダイアログ（`*.json`）。DIFF 基準を更新 |
| **RESET** | ボタン | 工場出荷状態へ | INIT 相当。DIFF 基準を更新 |
| **DIFF** | トグル | 基準音色との比較 | ON: 起動時または最後の RESET/LOAD 時の音色を適用し、他コントロールをロック。OFF: 復帰。Space キーでも切替 |
| **MASTER** | 水平スライダー | マスター音量 | 0〜100% 表示（内部 0.00〜1.00、既定 85%） |

### プリセット

| 種別 | 名称 | 備考 |
| ---- | ---- | ---- |
| 内蔵 | INIT / PAD / BASS / LEAD | 選択即反映 |
| ユーザー | 任意名 | 保存先 `%APPDATA%/NEXUS OSC/Presets/*.json` |

SAVE / SAVE AS / LOAD / RESET / プリセット選択後はベースラインを更新し、未編集なら SAVE / SAVE AS は無効。

---

## 1b. DIFF 比較モード

| 項目 | 内容 |
| ---- | ---- |
| 基準の記録 | 起動時、**RESET**、**LOAD**、ユーザープリセット **LOAD** 後 |
| DIFF ON | 基準の全パラメータを適用（**MASTER** は保持）。UI は **ALL OFF / MASTER / MIDI IN / DIFF** のみ操作可 |
| DIFF OFF | DIFF ON 前のスナップショットへ復帰（MASTER は DIFF 中の値を維持） |
| キーボード | Space キーで ON/OFF 切替（モーダルダイアログ表示中は無効） |
| 演奏 | 仮想キーボード・**PC キーボード（ON 時）**・外部 MIDI は DIFF 中も有効 |

---

## 2. OSCILLATOR（オシレータ）

| UI | 種別 | 範囲 / 選択肢 | 既定値 | 備考 |
| -- | ---- | ------------- | ------ | ---- |
| **OSC1 波形** | 4 ボタン | Sine / Saw / Square / Triangle | Saw | 排他選択 |
| **OSC2 波形** | 4 ボタン | 同上 | Square | 排他選択。**同じ波形を再クリックで OFF**（`osc2Enabled=false`）。OFF 時は DET2・MIXER の OSC2 が非活性 |
| **TUNE** | ノブ | ±12 semitones | 0 | OSC1/OSC2 共通の粗い音程 |
| **TUNE RESET** | ボタン | — | TUNE→0 | |
| **FINE** | ノブ | ±100 cent | 0 | 小数 **2 桁**表示 |
| **FINE RESET** | ボタン | — | FINE→0 | |
| **DET2** | ノブ | ±100 cent | 0 | OSC2 のみ。小数 **2 桁**表示。OSC2 OFF 時は非活性 |
| **DET2 RESET** | ボタン | — | DET2→0 | OSC2 OFF 時は非活性 |

---

## 3. MIXER（ミキサー）

| UI | 種別 | 範囲 | 既定値 | 備考 |
| -- | ---- | ---- | ------ | ---- |
| **OSC1** | ノブ | 0.00〜1.00 | 0.85 | OSC1 レベル |
| **OSC2** | ノブ | 0.00〜1.00 | 0.00 | OSC2 レベル。OSC2 波形 OFF 時は非活性 |
| **SUB** | ノブ | 0.00〜1.00 | 0.00 | サブオシレータ（OSC1 波形のオクターブ下） |
| **NOISE** | ノブ | 0.00〜1.00 | 0.00 | ホワイトノイズ |
| **SUB**（枠） | 表示 | — | — | 角括弧風フレーム内に -1 / -2 OCT ボタン |
| **-1 OCT** | ボタン | — | 選択中 | サブを 1 オクターブ下 |
| **-2 OCT** | ボタン | — | — | サブを 2 オクターブ下 |
| **GLIDE** | ノブ | 0.00〜2.00（秒表示） | 0.00 | ポルタメント（時定数型指数グライド）。MONO + レガートで有効 |
| **V-A** | ノブ | 0.00〜1.00 | 0.60 | ベロシティ → アンプ音量の深さ |
| **V-F** | ノブ | 0.00〜1.00 | 0.35 | ベロシティ → フィルタカットオフの深さ |

---

## 4. FILTER（フィルタ）

| UI | 種別 | 範囲 | 既定値 | 備考 |
| -- | ---- | ---- | ------ | ---- |
| **CUT** | ノブ | 80〜18000 Hz（対数スケール） | 6000 | ローパスカットオフ |
| **RES** | ノブ | 0.10〜8.00 | 0.71 | レゾナンス |
| **ENV** | ノブ | 0.00〜1.00 | 0.50 | フィルタ EG の変調量 |
| **KEY** | ノブ | 0.00〜1.00 | 0.00 | キートラッキング（0 で無効） |
| **Filter EG グラフ** | 表示 | — | — | FA/FD/FS/FR に連動する ADSR 曲線 |
| **FA** | ノブ | 0.001〜5.0 s | 0.02 | Filter Attack |
| **FD** | ノブ | 0.001〜5.0 s | 0.35 | Filter Decay |
| **FS** | ノブ | 0.00〜1.00 | 0.35 | Filter Sustain（レベル） |
| **FR** | ノブ | 0.001〜5.0 s | 0.40 | Filter Release |

フィルタ種類: ステート変数ローパス（UI 上の切替なし）。

---

## 5. AMPLIFIER（アンプ）

| UI | 種別 | 範囲 | 既定値 | 備考 |
| -- | ---- | ---- | ------ | ---- |
| **Amp EG グラフ** | 表示 | — | — | A/D/S/R に連動する ADSR 曲線 |
| **A** | ノブ | 0.001〜5.0 s | 0.01 | Amp Attack |
| **D** | ノブ | 0.001〜5.0 s | 0.15 | Amp Decay |
| **S** | ノブ | 0.00〜1.00 | 0.70 | Amp Sustain（レベル） |
| **R** | ノブ | 0.001〜5.0 s | 0.25 | Amp Release |

---

## 6. LFO

2 段独立（**LFO1** / **LFO2**）。各段に RATE / DEPTH、ルーティング 3 種、RATE 連動 LED。

### LFO1

| UI | 種別 | 範囲 | 既定値 | 備考 |
| -- | ---- | ---- | ------ | ---- |
| **RATE** | ノブ | 0.05〜20.0 Hz | 4.00 | |
| **RATE LED** | 表示 | — | — | 位相に連動して点滅（LFO1） |
| **DEPTH** | ノブ | 0.00〜1.00 | 0.30 | |
| **PITCH** | トグル | ON/OFF | OFF | 音程変調 |
| **FILTER** | トグル | ON/OFF | ON | カットオフ変調 |
| **AMP** | トグル | ON/OFF | OFF | 音量変調 |

### LFO2

| UI | 種別 | 範囲 | 既定値 | 備考 |
| -- | ---- | ---- | ------ | ---- |
| **RATE** | ノブ | 0.05〜20.0 Hz | 0.80 | |
| **RATE LED** | 表示 | — | — | LFO2 用 |
| **DEPTH** | ノブ | 0.00〜1.00 | 0.00 | |
| **PITCH** | トグル | ON/OFF | OFF | |
| **FILTER** | トグル | ON/OFF | OFF | |
| **AMP** | トグル | ON/OFF | OFF | |

同一宛先への LFO1 + LFO2 変調は加算。

---

## 7. SYSTEM

| UI | 種別 | 説明 |
| -- | ---- | ---- |
| **MIDI IN** | コンボ | MIDI 入力デバイス選択 |
| **ステータス行** | ラベル | MIDI 接続状態、またはコントロールホバー時のヘルプ文 |

### MIDI IN の選択肢

| 項目 | 動作 |
| ---- | ---- |
| **All Inputs** | 検出された全 MIDI 入力を同時受信（既定） |
| 個別デバイス名 | 選択した 1 デバイスのみ |
| （デバイスなし） | コンボは "No devices" 表示 |

ステータス例: `MIDI :: ALL INPUTS ACTIVE` / `MIDI :: <デバイス名>` / `MIDI :: NOT CONNECTED`

---

## 8. 仮想キーボード・PC キーボード（ASDF）

### 8a. 仮想キーボード（`MidiKeyboardComponent`）

| 項目 | 内容 |
| ---- | ---- |
| 操作 | マウスクリックで Note On/Off |
| 音域 | MIDI 36〜96（C1〜C7 付近） |
| 表示 | 押下中キーのハイライト |
| 併用 | 外部 MIDI と同時に `MidiKeyboardState` へマージ |

### 8b. PC キーボード演奏

| UI | 種別 | 説明 |
| -- | ---- | ---- |
| **ON / OFF** | ラジオ式トグル（上下配置） | **ON**: PC キー → MIDI マッピングを有効化。**OFF**: マッピング解除（押下中の PC キー音は停止） |
| **PC キー図** | 表示 | 2 段のキー配列。押下中キーをハイライト（OS のキー状態を参照） |

| 項目 | 内容 |
| ---- | ---- |
| 既定 | 起動時 **ON** |
| キー配列 | JUCE 標準 QWERTY（`awsedftgyhujkolp;`） |
| 下段（白鍵） | **A S D F G H J K L +** — 物理キー **A〜L** と **;**（**+** 表示） |
| 上段（黒鍵） | **S D T Y U O P**（図上ラベル）— 物理キー **W E T Y U O P**（上段 **S/D** は **W/E** の位置表示） |
| フォーカス | PC キーで**発音**するには `MidiKeyboardComponent` がキーボードフォーカスを持つ必要がある |
| フォーカス移動 | **ON** 選択時、起動後（ウィンドウ表示完了後）、**ON** 再クリック、**PC キー図**クリックで仮想鍵盤へフォーカス |
| 実装 | `Main.cpp` — `setPcKeyboardEnabled`, `applyDefaultPcKeyMappings`, `requestPcKeyboardFocus` |
| 表示 | `UI/PcKeyboardDisplay.*` — 30 Hz でキー状態をポーリングして描画 |

**Space** キーは PC 演奏ではなく **DIFF** 切替専用（`DiffShortcutKeyListener`）。ASDF 演奏と競合しない。

オーディオデバイスやバッファサイズの UI は無い（OS / JUCE デフォルト出力）。

---

## 8c. ト音記号五線譜（`TrebleStaffDisplay`）

仮想鍵盤と PC キー図の間に配置。発音中ボイスの MIDI ノートを **音名（pitch class）単位** で五線譜上に表示する。

| UI | 種別 | 説明 |
| -- | ---- | ---- |
| **♯** | トグル（上） | 臨時記号をシャープ表記に切替（ラジオグループ 9102） |
| **♭** | トグル（下） | 臨時記号をフラット表記に切替。**起動時既定** |
| 五線譜本体 | 表示 | ト音記号・五線・中央ド用補助線（常時表示）・音符・臨時記号 |

### 表示仕様

| 項目 | 内容 |
| ---- | ---- |
| 音域 | 中央ド（下加線）〜 その 1 オクターブ上の変ロ長音付近（`staffStep` -2〜11） |
| 入力 | `MainComponent::collectActiveMidiNotes()` — 発音中ボイスの MIDI ノート番号 |
| 統合 | 同じ pitch class（C, C♯, …）は **1 つの音符** にまとめる |
| 最低音 | 実際の最低音は正しい音高で表示 |
| それ以外 | 最低音より下に折り畳まれる音は **1 オクターブ上** にずらして重なりを避ける |
| 音符 | 楕円形符頭、高さ **9px** 固定 |
| 横位置 | 線上の音（ド・ミ・ソ等）は左寄せ、線間の音（レ・ファ・ラ等）は右寄せ（段階的オフセット） |
| 臨時記号 | Bravura グリフ（♯ `U+E262` / ♭ `U+E260`）。複数同時は **調号順** に左から右へ横にずらして重なり回避 |
| ト音記号 | Bravura グリフ（`U+E050`） |
| 更新 | 30 Hz タイマーで `repaint` |

### レイアウト定数（参考）

| 定数 | 値 | 備考 |
| ---- | -- | ---- |
| `kTrebleStaffWidth` | 162 px | `Main.cpp` |
| `kAccidentalToggleColumnWidth` | 18 px | ♯/♭ ボタン列 |
| `kClefAreaWidth` | 36 px | ト音記号領域 |

### 実装ファイル

- `Source/UI/TrebleStaffDisplay.*` — 描画・♯/♭ 切替
- `Source/UI/FuturisticLookAndFeel.cpp` — ♯/♭ ボタンのラベル描画（`staffAccidentalToggle`）
- `Resources/Bravura.otf` — `AnalogSynthFonts` バイナリデータとしてリンク

---

## 9. 視覚フィードバック（操作以外の UI 表示）

| 機能 | 説明 |
| ---- | ---- |
| **EG 再生ヘッド** | 演奏中、Filter / Amp の EG グラフ上に点（最大 16）が移動 |
| **LFO RATE LED** | LFO1/LFO2 各 1 個。RATE に同期した点滅 |
| **ト音記号五線譜** | 発音中の pitch class を音符で表示（`TrebleStaffDisplay`） |
| **ノブ値ラベル** | 各ノブ下に数値表示（CUT は Hz 整数など） |
| **モジュールパネル枠** | OSC / MIXER / FILTER / AMP / LFO / SYSTEM の区分表示 |

---

## 10. ヘルプ

| 項目 | 内容 |
| ---- | ---- |
| 言語 | 日本語 |
| 表示場所 | SYSTEM フッターのステータス行 |
| フォント | **14pt**（`Yu Gothic UI` / システムメッセージ用） |
| トリガー | コントロール・パネル・波形ボタン等にマウスホバー |
| 既定文 | 「コントロールにマウスを合わせると説明が表示されます。」 |
| 複数行 | 改行を含むヘルプは 2 行表示に対応 |

パネル全体・各ノブ・波形・LFO ルート・プリセット・MIDI 等に個別ヘルプ文あり（`HelpStrings.h`）。

---

## 11. 演奏仕様（UI から分かる範囲）

| 項目 | 内容 |
| ---- | ---- |
| ポリフォニー | 最大 **16 ボイス**（サブタイトル表示。UI で変更不可） |
| MONO | トグルで切替。レガート時 GLIDE が効く |
| ベロシティ | 外部 MIDI のベロシティを V-A / V-F が反映 |
| ALL OFF | 即時全停止 |
| OSC2 OFF | 波形再クリックで OSC2 出力停止（`osc2Enabled`） |

---

## 12. セッション永続化

| 項目 | 内容 |
| ---- | ---- |
| 保存先 | `%APPDATA%/NEXUS OSC/session.json` |
| タイミング | アプリ終了時 |
| 保存内容 | 全シンセパラメータ、プリセット選択インデックス、MIDI IN 設定、ウィンドウ位置・サイズ |
| 復元 | 次回起動時に `AppState::load()` で適用 |

---

## UI に無い項目（参考）

本節は **画面上にコントロールが無い** ものの整理。  
**実装済み**（動作はするが UI がない）と **未実装**（コードにも無い／スタブのみ）を分けて記載する。

### 実装済み（UI なし）

| カテゴリ | 内容 | 備考 |
| -------- | ---- | ---- |
| オーディオ出力 | Windows デフォルトデバイスへステレオ出力 | JUCE 経由（通常 WASAPI）。**デバイス選択・バッファサイズの UI は無い** |
| MIDI 入力 | 外部 MIDI の受信・演奏 | **MIDI IN** のみ UI あり。チャンネルフィルタ等の UI は無い |
| ポリフォニー | 最大 16 ボイス | サブタイトル表記のみ。**発音数の動的表示 UI は無い** |
| セッション | 終了時に `session.json` へ全パラメータ・プリセット・MIDI・ウィンドウを保存 | UI からの手動保存は無し |
| PC キーボード演奏 | ASDF 等で演奏、ON/OFF、キー図表示 | **ON / OFF**、右端 PC キー図。Space は DIFF 専用 |
| ト音記号五線譜 | 発音中音程のリアルタイム表示、♯/♭ 切替 | `TrebleStaffDisplay`。セッション保存対象外 |
| DIFF 比較 | 基準音色との A/B 切替 | **DIFF** ボタン / Space キー |

### 未実装

| カテゴリ | 内容 | 備考 |
| -------- | ---- | ---- |
| オーディオ | ASIO 出力、オーディオ設定画面 | ASIO はビルド未リンク。`AudioDeviceSelectorComponent` 未使用 |
| MIDI 演奏 | ピッチベンド、Mod ホイール | `pitchWheelMoved` / `controllerMoved` は空実装 |
| プラグイン | VST3 / CLAP | Standalone EXE のみ |
| FX | コーラス / ディレイ / リバーブ | 信号チェーンに無し |
| DSP | SmoothedValue、実効 Cutoff Hz 表示 | パラメータは atomic 直読み。モジュレーション後の Hz 表示なし |
| その他 | アルペジエータ、MPE、ポリフォニー数の動的表示 | 将来候補 |

詳細は [ARCHITECTURE.md](ARCHITECTURE.md) の「現状の制約と今後の拡張」を参照。

---

## 関連ドキュメント

| ファイル | 内容 |
| -------- | ---- |
| [README.md](README.md) / [README.en.md](README.en.md) | 概要・ビルド・使い方 |
| [ARCHITECTURE.md](ARCHITECTURE.md) / [ARCHITECTURE.en.md](ARCHITECTURE.en.md) | 信号フロー・スレッド・実装 |
| [SPEC.md](SPEC.md) | 本ドキュメント（UI 機能仕様） |
